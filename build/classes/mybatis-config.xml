<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration 
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 读取配置文件 -->
	<!-- <properties resource="jdbc.properties"> </properties> -->
	<!-- 为该包下面的所有类配置别名，别名的名称为类的名称的首字符小写 ,如com.chinasoft.pojo.User的别名为user -->
	<typeAliases>
		<package name="com.Chinahard.pojo" />
	</typeAliases>

	<!-- 配置数据源 -->
	<!-- <environments default="development"> <environment id="development"> 
		<transactionManager type="JDBC"/> <dataSource type="POOLED"> <property name="driver" 
		value="${jdbc_driver}"></property> <property name="url" value="${jdbc_url}"></property> 
		<property name="username" value="${jdbc_username}"></property> <property 
		name="password" value="${jdbc_password}"></property> </dataSource> </environment> 
		</environments> -->

	<!-- 读取mybatis的映射文件 -->
	<!-- <mappers> <mapper resource="com/chinasoft/dao/UserDao.xml"/> </mappers> -->


</configuration>